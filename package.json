{
  "name": "fs_lab-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm --workspace=frontend run dev",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  }
}
